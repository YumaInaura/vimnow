#!/bin/bash -eu

readonly save_dir=${VIMNOW_PATH:-~/vimnow}

mkdir -p "$save_dir"

file_extension=''

for option in "$@"; do
  [ "$option" = '--sh' ] && file_extension='.sh'
done

vim "$save_dir"/$(date -u | sed 's/ /-/g')-$(date +%s)"$file_extension"

